{
  "name": "dreary-disk",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "dependencies": {
    "astro": "6.3.6",
    "@astrojs/cloudflare": "13.5.2"
  },
  "devDependencies": {
    "wrangler": "4.83.0"
  }
}